# ---- Core dependencies (always needed) ----
numpy>=1.23,<2.0
scipy
Pillow
cython
matplotlib
scikit-image>=0.19
opencv-python
Shapely>=2.0
packaging
tqdm
PyYAML

# ---- YOLOv8 backend (default) ----
ultralytics>=8.0,<9.0
torch>=2.0

# ---- Mask R-CNN backend (legacy, optional) ----
# Uncomment the lines below if you need --backend maskrcnn.
# tensorflow>=2.10,<2.16
# h5py>=3.1
# imgaug
# IPython[all]
